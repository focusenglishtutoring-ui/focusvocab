## Packages
framer-motion | Smooth transitions between learning cards and states
clsx | Utility for constructing className strings conditionally (often paired with tailwind-merge)
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
The app relies on a single unit JSON structure fetched from /api/unit.
No authentication is required.
Progress is ephemeral (reset on refresh).
